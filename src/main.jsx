import React from "react";
import { createRoot } from "react-dom/client";
import TipKiosk from "./TipKiosk.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TipKiosk />
  </React.StrictMode>
);
